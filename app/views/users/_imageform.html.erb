      <%= form_with(url: user_path(@user), method: :patch, local: true) do |f| %>
  <%= f.file_field :cover, class: 'custom-background-input', id: "pictureInput", onchange: 'this.form.submit();' %>
<%end%>
