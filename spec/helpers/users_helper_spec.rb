# require "rails_helper"

# RSpec.describe UsersHelper, type: :helper do
#   let(:user) { User.new(id: 1, name: "Patrick Nyatindo", email: "pato@gmail.com", username: "pato") }
#   let(:current_user) { user }

#   it "Should only return render if current user" do
#     expect(helper.background_update(user)).to eq(nil)
#   end
# end
